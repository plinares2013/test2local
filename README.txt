Testing with a .txt document
Github

Adding this line on 64b machine.

Adding the line with dimension5100

This line is from the main branch, just after (Adding the line with dimension5100) while the same file is modified in the barnch 'test'.